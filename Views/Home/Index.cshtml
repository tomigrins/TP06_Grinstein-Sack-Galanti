<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <div class="container">
        <h1>Bienvenido al sitio del alumno</h1>
        <img src="/static/img/home.jpg" alt="Imagen de bienvenida">
        <p>Este sitio permite consultar tus datos personales, familiares y de interés como alumno.</p>

        @if (ViewBag.usuarioNom != null){
            <p>Usuario logueado: <strong>@ViewBag.usuarioNom</strong></p>
            <a href="@Url.Action("Desloguearse", "Index")">Desloguearse</a>
        }
        else{
            <a href="@Url.Action("SignIn")">Iniciar sesión</a>
        }
    </div>
</body>
</html>
